{
  "enabled": {
    "type": "boolean",
    "required": false,
    "default": true,
    "description": "Enable or disable the widget"
  },
  "message1_text": {
    "type": "string",
    "required": true,
    "maxLength": 500,
    "description": "Text for the first message",
    "example": "Free shipping on orders over $50"
  },
  "message1_icon": {
    "type": "string",
    "required": false,
    "maxLength": 200,
    "description": "Emoji or image URL for message 1",
    "example": "🚚"
  },
  "message1_iconPosition": {
    "type": "string",
    "required": false,
    "enum": ["left", "right"],
    "default": "left",
    "description": "Icon position for message 1"
  },
  "message2_text": {
    "type": "string",
    "required": false,
    "maxLength": 500,
    "description": "Text for the second message (optional)",
    "example": "10% off on your first purchase"
  },
  "message2_icon": {
    "type": "string",
    "required": false,
    "maxLength": 200,
    "description": "Emoji or image URL for message 2",
    "example": "💰"
  },
  "message2_iconPosition": {
    "type": "string",
    "required": false,
    "enum": ["left", "right"],
    "default": "left",
    "description": "Icon position for message 2"
  },
  "message3_text": {
    "type": "string",
    "required": false,
    "maxLength": 500,
    "description": "Text for the third message (optional)",
    "example": "New arrivals every week"
  },
  "message3_icon": {
    "type": "string",
    "required": false,
    "maxLength": 200,
    "description": "Emoji or image URL for message 3",
    "example": "✨"
  },
  "message3_iconPosition": {
    "type": "string",
    "required": false,
    "enum": ["left", "right"],
    "default": "left",
    "description": "Icon position for message 3"
  },
  "message4_text": {
    "type": "string",
    "required": false,
    "maxLength": 500,
    "description": "Text for the fourth message (optional)",
    "example": "Shop now and save"
  },
  "message4_icon": {
    "type": "string",
    "required": false,
    "maxLength": 200,
    "description": "Emoji or image URL for message 4",
    "example": "🛍️"
  },
  "message4_iconPosition": {
    "type": "string",
    "required": false,
    "enum": ["left", "right"],
    "default": "left",
    "description": "Icon position for message 4"
  },
  "message5_text": {
    "type": "string",
    "required": false,
    "maxLength": 500,
    "description": "Text for the fifth message (optional)",
    "example": "24/7 customer support"
  },
  "message5_icon": {
    "type": "string",
    "required": false,
    "maxLength": 200,
    "description": "Emoji or image URL for message 5",
    "example": "💬"
  },
  "message5_iconPosition": {
    "type": "string",
    "required": false,
    "enum": ["left", "right"],
    "default": "left",
    "description": "Icon position for message 5"
  },
  "separator": {
    "type": "string",
    "required": false,
    "maxLength": 10,
    "default": "•",
    "description": "Character to separate messages",
    "example": "•"
  },
  "backgroundColor": {
    "type": "string",
    "required": false,
    "pattern": "^#[0-9A-Fa-f]{6}$",
    "default": "#000000",
    "description": "Background color (hex)",
    "example": "#0066cc"
  },
  "textColor": {
    "type": "string",
    "required": false,
    "pattern": "^#[0-9A-Fa-f]{6}$",
    "default": "#FFFFFF",
    "description": "Text color (hex)",
    "example": "#FFFFFF"
  },
  "fontSize": {
    "type": "string",
    "required": false,
    "enum": ["12px", "14px", "16px", "18px"],
    "default": "14px",
    "description": "Font size"
  },
  "speed": {
    "type": "string",
    "required": false,
    "enum": ["slow", "normal", "fast"],
    "default": "normal",
    "description": "Animation speed"
  },
  "position": {
    "type": "string",
    "required": false,
    "enum": ["top", "bottom"],
    "default": "top",
    "description": "Widget position on page"
  },
  "pauseOnHover": {
    "type": "boolean",
    "required": false,
    "default": true,
    "description": "Pause animation when mouse hovers"
  },
  "showMarquee": {
    "type": "boolean",
    "required": false,
    "default": true,
    "description": "Enable scrolling animation"
  },
  "containerId": {
    "type": "string",
    "required": false,
    "default": "marquee-notification-bar",
    "description": "Custom HTML container ID (advanced)",
    "example": "marquee-notification-bar"
  }
}
